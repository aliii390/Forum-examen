{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}
<main class="flex flex-col justify-center lg:flex lg:justify-center">
    <div class="flex flex-wrap gap-2 justify-center p-3">
        <a href="#"><span class="bg-[#F48023] text-white px-3 py-1 rounded-full text-sm">Tech</span></a>
        <a href="#"><span class="bg-[#F48023] text-white px-3 py-1 rounded-full text-sm">Programmation</span></a>
        <a href="#"><span class="bg-[#F48023] text-white px-3 py-1 rounded-full text-sm">CyberSécurité</span></a>
    </div>
<section class="flex flex-col items-center justify-center min-h-[80vh] p-4 sm:p-6 md:p-8 gap-8 sm:gap-12 md:gap-16">
    <article class="shadow-lg flex flex-col gap-6 sm:gap-8 p-6 sm:p-8 md:p-11 rounded-lg w-full max-w-md md:max-w-lg lg:max-w-xl mx-auto">
        <div class="flex items-center gap-4 sm:gap-6 md:gap-8 relative">
            <img 
                class="rounded-full w-16 sm:w-20 md:w-24 aspect-square object-cover" 
                src="{{ asset('img/ammar.png') }}" 
                alt="Profile"
            />
            <div class="flex flex-col">
                <h3 class="text-lg sm:text-xl md:text-2xl font-roboto font-light">Oussama</h3>
                <p class="text-sm text-gray-600 font-roboto">il y a 3 min</p>
            </div>
            <div class="relative">
                <i class="fa-solid fa-plus cursor-pointer hover:scale-110 transition-transform duration-200" 
                   style="color: #000000;" 
                   onclick="togglePopup(event)"></i>
                <div class="popup hidden absolute bg-white shadow-xl rounded-lg p-3 -top-16 right-0 w-32 transform transition-all duration-200 ease-in-out">
                    <div class="relative">
                        <!-- Flèche du popup -->
                        <div class="absolute w-3 h-3 bg-white transform rotate-45 -bottom-5 right-3"></div>
                        <!-- Contenu -->
                        <div class="flex flex-col gap-2">
                            <p class="text-sm text-gray-600 hover:text-[#F48023] cursor-pointer transition-colors duration-200 flex items-center gap-2">
                                <i class="fas fa-user-plus text-xs"></i>
                                <span>Ajoutez</span>
                            </p>
                            <p class="text-sm text-gray-600 hover:text-red-500 cursor-pointer transition-colors duration-200 flex items-center gap-2">
                                <i class="fas fa-ban text-xs"></i>
                                <span>Blocké</span>
                            </p>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <div class="flex flex-col space-y-2">
            <p class="text-xl sm:text-2xl md:text-3xl font-sans">Mettre le titre ici</p>
            <p class="text-sm sm:text-base text-black font-roboto font-light">
                mettre la description la solution etc...
            </p>
        </div>

        <div class="flex flex-wrap gap-2 items-start">
            <span class="bg-gray-100 text-gray-600 px-2 sm:px-3 py-1 rounded-lg text-xs sm:text-sm">category1</span>
            <span class="bg-gray-100 text-gray-600 px-2 sm:px-3 py-1 rounded-lg text-xs sm:text-sm">category2</span>
            <span class="bg-gray-100 text-gray-600 px-2 sm:px-3 py-1 rounded-lg text-xs sm:text-sm">category3</span>
        </div>

        <a href="{{ path('app_questions') }}" 
           class="inline-flex items-center justify-center px-4 sm:px-6 md:px-8 py-2 sm:py-2.5 md:py-3 
                  text-xs sm:text-sm md:text-base font-medium
                  text-white bg-[#F48023] hover:bg-[#da7220] 
                  rounded-lg transition-all duration-200 ease-in-out
                  transform hover:scale-105
                  shadow-md hover:shadow-lg
                  w-fit">
            <span>En savoir plus</span>
            <svg xmlns="http://www.w3.org/2000/svg" class="w-4 h-4 ml-2" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14 5l7 7m0 0l-7 7m7-7H3" />
            </svg>
        </a>        
    </article>
</section>



</main>


<script> 

function togglePopup(event) {
    const icon = event.currentTarget;
    const popup = icon.parentElement.querySelector('.popup');

    if (popup.classList.contains('hidden')) {
        // Afficher le popup avec animation
        popup.classList.remove('hidden');
        popup.classList.add('visible');
        popup.classList.add('opacity-0');
        setTimeout(() => {
            popup.classList.remove('opacity-0');
            popup.classList.add('opacity-100');
        }, 50);
    } else {
        // Cacher le popup avec animation
        popup.classList.remove('opacity-100');
        popup.classList.add('opacity-0');
        setTimeout(() => {
            popup.classList.add('hidden');
            popup.classList.remove('visible');
        }, 200);
    }
}

document.addEventListener('click', function(event) {
    const isClickInside = event.target.closest('.popup, .fa-plus');
    if (!isClickInside) {
        document.querySelectorAll('.popup').forEach(popup => {
            popup.classList.remove('opacity-100');
            popup.classList.add('opacity-0');
            setTimeout(() => {
                popup.classList.add('hidden');
                popup.classList.remove('visible');
            }, 200);
        });
    }
});
</script>



 
 
{% endblock %}